import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#FF7D29",
        },
    },
    typography: {
        fontFamily: "Nanum Brush Script", // Set the font family here
    },
});
